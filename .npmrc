# Permite que estas dependencias ejecuten sus scripts de instalación.
# Sin esto, esbuild (el bundler de Angular) no descarga su binario nativo
# y `ng build` falla con "The package esbuild could not be found".
# Se versiona para que los 5 integrantes no tengan que aprobarlas a mano.
enable-pre-post-scripts=true
