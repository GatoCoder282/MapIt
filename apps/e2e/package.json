{
  "name": "@mapit/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Tests end-to-end con Playwright: console y public-web.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^24.10.0"
  }
}
