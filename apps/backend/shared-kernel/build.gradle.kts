// shared-kernel — tipos y contratos comunes a todos los módulos.
// Igual que la capa `domain`: Java puro, sin frameworks.
// Es la raíz del grafo de dependencias: no depende de ningún otro módulo.
plugins {
    id("mapit.hexagon-domain")
}
