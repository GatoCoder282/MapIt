{
  "name": "@mapit/map-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Puerto del motor de mapa + adaptadores intercambiables.",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "echo \"(sin tests todavía en @mapit/map-engine)\"",
    "typecheck": "tsc -p tsconfig.lib.json --noEmit"
  }
}
