{
  "name": "@mapit/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Cliente HTTP generado desde el contrato OpenAPI + interceptores.",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "echo \"(sin tests todavía en @mapit/api-client)\"",
    "typecheck": "tsc -p tsconfig.lib.json --noEmit"
  }
}
