{
  "name": "@mapit/feature-flags",
  "version": "0.1.0",
  "private": true,
  "description": "Feature toggles en el frontend, vía unleash-proxy.",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "echo \"(sin tests todavía en @mapit/feature-flags)\"",
    "typecheck": "tsc -p tsconfig.lib.json --noEmit"
  }
}
