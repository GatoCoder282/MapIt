{
  "name": "@mapit/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Autenticación JWT: guards, sesión y renovación de token.",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "echo \"(sin tests todavía en @mapit/auth)\"",
    "typecheck": "tsc -p tsconfig.lib.json --noEmit"
  }
}
