# MapIt

El contexto para agentes vive en **[`AGENTS.md`](AGENTS.md)** — es el estándar abierto que
leen varias herramientas (Claude Code, Codex, Cursor), así que no lo duplicamos aquí.

Cada sección del monorepo tiene además su propio `AGENTS.md` con las reglas de esa zona:
`apps/backend/`, `apps/console/`, `apps/public-web/`, `apps/e2e/`, `libs/`,
`packages/api-contract/`, `infra/`, `specs/`, `docs/`.

**Empieza por [`AGENTS.md`](AGENTS.md).** Tiene una tabla de ruteo: busca tu tarea y te
dice qué leer y qué agente o skill usar.
